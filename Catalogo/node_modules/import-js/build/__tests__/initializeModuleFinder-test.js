"use strict";

var _initializeModuleFinder = _interopRequireDefault(require("../initializeModuleFinder"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

it('fails if required files are missing', function () {
  var bogusDir = '/tmp/sherlock-holmes/pippi-longstocking';
  return (0, _initializeModuleFinder.default)(bogusDir).then(function () {
    expect(true).toEqual(false); // we shouldn't end up here
  }).catch(function (error) {
    expect(error.message).toEqual("ModuleFinder is disabled for ".concat(bogusDir, " (none of .importjs.js, package.json were found)."));
  });
});